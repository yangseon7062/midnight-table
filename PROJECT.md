# 심야 테이블 — 프로젝트 현황

> **작업을 시작하기 전에 이 문서를 먼저 읽는다.**
> 아키텍처나 설계 결정에 영향을 주는 수정을 하면 **그 자리에서 이 문서도 함께 갱신한다.**
> 이 문서가 낡으면 이후의 모든 판단이 낡은 정보 위에서 이뤄진다.

마지막 갱신: 2026-09-18

---

## 1. 한 줄 요약

2D 저택 공간을 직접 걸어 다니다 테이블에 앉아 보드게임을 하는 실시간 멀티플레이 웹 플랫폼.
현재 탑재 모듈은 **머더미스터리** 하나, 테스트 시나리오는 **「해무관의 마지막 잔」**(2인·75분).
플랫폼은 처음부터 **장르 중립**으로 설계되어 있어 다른 장르를 모듈로 얹을 수 있다.

## 2. 우선순위 원칙 (최초 개발 프롬프트, 유지보수 단계에도 그대로 적용)

순서를 어기지 않는다. 아래로 갈수록 후순위다.

1. **재미와 촉감** — 버튼 한 번에 결과가 나오는 게 아니라 손으로 만지는 과정이 있어야 한다
2. **멀티게임 구조의 정확성 + 안정적인 실시간 동기화 프레임**
3. **장르 고유의 판단(정보 공개·추리·투표)과 보상(엔딩) 설계**
4. 캐릭터·UI·사운드·화면 완성도
5. 시나리오 개수

그리고:

- 쉬운 MVP 수준에 스스로 상한을 두지 않는다. 요구사항은 출발점일 뿐이다.
- 장식이나 기능 개수로 품질을 대신하지 않는다.
- **작업 방식은 구현 → 실제 실행 → 관찰 → 디버깅 → 개선 → 재검증의 반복이다.** 한 번 고쳐서 동작하는 것처럼 보인다고 바로 완료 보고하지 않는다. 유지보수 단계에서도 동일하다.

## 3. 아키텍처

### 레이어 분리 — 이 프로젝트의 가장 중요한 구조

```
로비 레이어          방 · 이동 · 밀담 구역 · 채팅 · 음성 · 테이블(게임 고르기)
  ↕ GameHost 계약     ← 로비는 이 계약 너머의 내용을 해석하지 않는다
게임 모듈            머더미스터리 규칙 (설정집 · 공용집 · 단서 · 투표 · 엔딩)
```

로비 레이어(`server/src/platform`, `client/src/ui`, `client/src/world`)에는 **장르 단어가 하나도 없다.**
"설정집", "투표", "진범" 같은 말은 전부 `modules/murder-mystery` 안에만 있다.

모듈이 로비에 요청할 수 있는 것은 `GameHost`의 소수 항목뿐이다 — 상태 갱신 알림, 일회성 연출 이벤트, 시스템 메시지/토스트, 구역 목록과 격리 on/off, **지금 누가 어느 대화 채널에 있는지**(`channelOf`/`audienceOf`), 이름표 배지, 게임 종료, 저장 표시.
**이 계약을 넓히는 수정은 신중하게 한다.** 로비가 장르를 알기 시작하면 다음 장르를 얹을 때 전부 다시 짜야 한다.

새 장르 추가 절차:
1. `server/src/modules/<장르>/`에 `GameModuleDefinition` 구현 → `server/src/index.ts`에서 `modules.set(...)` 등록
2. `client/src/modules/<장르>/` UI 작성 → `client/src/modules/registry.ts` 등록

로비·테이블·방 목록·재접속은 손대지 않는다.

### 디렉터리

```
shared/            클라이언트·서버 공용 (한 벌의 진실)
  platform.ts        로비 프로토콜 (장르 개념 없음)
  world.ts           맵·충돌·구역 판정 — 서버 검증과 클라이언트 예측이 같은 코드를 쓴다
  mm/scenario.ts     시나리오 zod 스키마 + 참조 무결성 검증 (서버·에디터 공용)
  mm/view.ts         플레이어별로 걸러진 뷰 타입
server/src/
  platform/          세션 토큰, 방, 이동 검증, 구역 격리, 음성 시그널링, 재접속, 영속화
  modules/murder-mystery/  머더미스터리 규칙
  admin/api.ts       운영자 API (인증, 시나리오 CRUD, 이미지 업로드)
client/src/
  world/             캔버스 도트 렌더러, 예측 이동·보간
  audio/             절차적 효과음(WebAudio), 음성(WebRTC)
  ui/                타이틀·라운지·방 HUD·채팅·테이블
  modules/mm/        머더미스터리 UI
  editor/            운영자 시나리오 에디터
data/scenarios/    시나리오 JSON (에디터 저장 → 즉시 반영)
data/uploads/      단서·표지 이미지
data/runtime/      users.json · rooms.json (자동 생성, git 제외)
```

### 실시간 동기화

- 서버 스냅샷 15Hz(`s` 이벤트), 클라이언트 입력 20Hz, 원격 캐릭터는 110ms 버퍼로 보간
- 이동은 클라이언트가 즉시 예측하고, 서버가 속도(토큰 버킷)·벽 통과(`pathClear`)를 검증해 어긋나면 `m:fix`로 되돌린다

## 4. 핵심 설계 결정과 이유

수정 요청이 아래와 충돌하면 **조용히 바꾸지 말고 먼저 알리고 확인받는다.**

| 결정 | 이유 |
| --- | --- |
| **서버 권위.** 이동·정보 공개·카드·투표·확인·연장 모두 서버가 검증 | 클라이언트를 믿으면 추리 게임이 성립하지 않는다 |
| **비공개 정보는 `공개 대상 ∩ 열람 권한`으로 서버가 계산** | 권한 없는 클라이언트에는 데이터 자체가 도착하지 않는다. 화면에서 숨기는 방식은 개발자 도구로 뚫린다 |
| **GM은 시스템 권한이 아니라 그냥 캐릭터다** | 진행 설명은 설정집에 쓰면 된다. 특정 사용자에게 진행 권한을 주지 않는다 |
| **진행 권한자가 없다. 단계는 타이머 종료 또는 전원 확인으로 자동 진행** | 한 사람이 진행을 쥐면 나머지는 관객이 된다 |
| **피해자 역할은 시스템이 따로 다루지 않는다** | 캐릭터의 "투표 참여" 플래그를 끄면 된다. 특수 상태를 늘리지 않는다 |
| **밀담 구역에 요청/수락이 없다. 들어가면 그 안 사람끼리만 대화** | 실제로 자리를 옮기는 행동 자체가 게임의 일부다 |
| **밀담 구역에 인원 제한을 두지 않는다** (2026-09-18 결정) | 제한이 만드는 "못 들어감" 경험이 재미를 깎는다. 밖에서 인원 수가 보이는 표시는 유지한다 |
| **구역에는 언제든 들어갈 수 있고, 격리(밀담)만 단계에 따라 켜지고 꺼진다** | 구역은 저택의 방이지 잠긴 문이 아니다. 대신 격리가 꺼진 구역에 들어서면 "지금은 밀담이 되지 않습니다"라고 알려 착각을 막는다 |
| **자료를 「테이블에 펼쳐」 같은 자리 사람들 화면에 동시에 띄울 수 있다** | 말로만 설명하는 것보다 PT 하듯 보여 주는 편이 추리가 굴러간다. 보이는 범위는 대화와 같은 규칙(구역 격리)을 쓰고, 비공개 자료는 되돌릴 수 없으므로 한 번 더 확인받는다. 펼친 자료는 화면에 띄울 뿐이고 상대의 자료로 넘어가지는 않는다 |
| **봉투는 상시 UI가 아니라 도착하는 순간의 사건이다** | 우편함이 계속 떠 있으면 "처리할 일 목록"처럼 보인다. 단계 전환 때 자동으로 펼쳐지고, 닫으면 사건 파일·단서 보드로 들어간다. 첫 봉투의 밀랍 뜯기 연출은 도입부라 유지한다 |
| **음성은 WebRTC mesh, 서버는 같은 채널끼리 시그널만 중계** | 서버가 오디오를 중계하지 않아 비용·지연이 없다. 대신 인원이 늘면 부담이 커진다(6명 내외까지가 적정) |
| **타이머는 참고용이고 말을 막지 않는다. 누구나 연장 가능(쿨타임 10초)** | 시간에 쫓겨 대화가 끊기는 게 가장 나쁘다 |
| **회원가입 없음. 닉네임 + 브라우저 세션 토큰** | 진입 장벽을 없앤다. 새로고침·끊김에도 같은 캐릭터로 복귀 |
| **진행 중인 게임의 참가자는 부재 상태여도 방의 자리를 유지한다** | 부재자는 언제든 돌아와 이어서 플레이할 수 있으므로 그 자리를 남에게 내주면 정원을 넘는다. 정원 계산은 `Room.seatCount()` 한 곳만 쓴다 — 라운지 표시와 입장 판정이 어긋나면 안 된다 |
| **능력/아이템 카드의 효과는 시스템이 해석하지 않는다** | 시스템은 보유·사용 횟수만 관리하고 효과는 사람이 읽고 판단한다. 자유도를 위해 |
| **그래픽·사운드를 외부 에셋 없이 코드로 생성** | 라이선스 문제가 없고 시나리오마다 다시 만들 필요가 없다 |
| **Vite는 6.x로 고정** | 7.x는 Node 22.12+ 요구. 사용자 환경이 Node 22.11이라 경고 없이 빌드되게 내렸다. 함부로 올리지 않는다 |

### 촉감(우선순위 1번)의 구체적 형태

버튼 한 번으로 끝나는 동작을 만들지 않는다. 현재 구현된 것:
봉투 밀랍 봉인을 끌어 뜯기 · 설정집 모서리를 끌어 책장 넘기기 · 단서 확대와 돋보기 · 카드를 위로 끌어 테이블에 내려놓기 · 투표 도장을 끌어 찍고 길게 눌러 확정.

## 5. 알려진 제약과 미해결 항목

| 항목 | 현재 상태 | 영향 |
| --- | --- | --- |
| **게임 상태 저장** | 메모리 + 3초마다 JSON 스냅샷(`data/runtime/`) | 서버 재시작은 견디지만 **디스크가 휘발하는 무료 PaaS에서는 유실된다.** 외부 배포 시 외부 저장소로 전환 필요 |
| **TURN 연동** | 계정·키는 발급받아 `.env`에 보관, 서버 연동은 미구현 | 다른 망 사이 음성이 붙지 않는 경우(전체의 10~20%) 대비가 아직 없음 |
| **배포** | 외부 배포 없음. 로컬 실행만 | 아래 6번 참고 |
| **장애 감지** | 없음 | 서버가 죽어도 사용자가 말하기 전까지 아무도 모름 |
| **음성 확장성** | mesh 구조 | 인원이 6명을 크게 넘으면 클라이언트 부담. 그 규모가 필요해지면 SFU 검토 |
| **모바일** | 검증하지 않음 | 데스크톱 기준으로만 확인됨 |

### 리전 제약 (외부 배포 시)

- **앱 서버 ↔ DB/Redis는 반드시 같은 리전.** 한 요청에 여러 번 왕복하므로 리전이 어긋나면 이동이 눈에 띄게 밀린다
- **앱 서버 ↔ TURN은 서로 무관하다.** 음성은 앱 서버를 거치지 않는다. 각자 플레이어에게 가까우면 된다
- 플레이어가 한국 기준이므로: 앱 서버는 **도쿄**(한국에서 30~40ms), TURN은 **서울**이 최선

## 6. 배포 환경

| | 상태 |
| --- | --- |
| **개발/작업 공간** | 클라우드 컨테이너의 `midnight-table/` (휘발성) |
| **사용자 PC** | `C:\Users\Admin\Desktop\online` — 수동 동기화본. 여기서 `npm start`로 로컬 실행 |
| **외부 배포** | **미정.** 도쿄 리전이 있는 무료 티어 PaaS 우선 검토. 결정하면 이 칸을 채운다 |
| **원격 저장소** | 없음. 깃허브 비공개 저장소 + 토큰 발급이 사용자 작업으로 대기 중 |

결정된 방침: **로컬 실행은 계속 유지하고 외부 배포를 병행한다.** 개발·확인은 로컬이 빠르다.

### TURN (음성 릴레이)

`.env`에만 보관하고 절대 커밋하지 않는다. 클라이언트에 키를 노출하지 않고 **서버가 대신 호출해 결과만 내려준다.**

1. **Metered** (서울 리전, 무료 500MB/월, 80·443 포트) — 1순위. REST로 동적 자격증명 발급
2. **ExpressTURN** (프랑스, 무료 1000GB/월, 3478 포트만) — 2순위 백업

TURN은 직접 연결이 실패한 소수 경로만 사용하므로 무료 한도로 충분하다. 유료 전환이 필요해지면 먼저 사용자에게 알린다.

## 7. 실행과 검증

```bash
npm install
npm test        # 전체 회귀 검증 (빌드 → 타입 → 규칙 → 서버 → 재접속 → 재시작 → 2인 UI → 3인 구역 → 에디터)
npm start       # http://localhost:3000, 에디터 /admin
npm run dev     # 핫 리로드 http://localhost:5173
```

세부 체크리스트는 `TESTING.md`, 대기 중인 버그·요청은 `ISSUES.md`.

## 8. 함께 보는 문서

- `README.md` — 실행 방법, 플레이 흐름, 에디터 사용법 (사용자/외부용)
- `TESTING.md` — 회귀 테스트 체크리스트
- `ISSUES.md` — 버그·기능 요청 목록
- `.claude/skills/` — 유지보수 워크플로우 스킬
