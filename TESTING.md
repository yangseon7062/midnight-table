# 회귀 테스트 체크리스트

**규칙**
1. 무언가를 수정하면 관련 항목만이 아니라 **`npm test` 전체를 다시 돌린다.** 통과 전에는 완료 보고도, 배포도 하지 않는다.
2. **기능을 추가하면 이 문서와 테스트 스크립트에 그 기능의 항목을 함께 추가한다.** 안 그러면 시간이 갈수록 회귀 테스트가 실제 기능을 못 덮는다.
3. 버그를 고치면 **그 버그를 잡아내는 검사**를 추가한다. 같은 자리에서 두 번 깨지지 않게.

```bash
npm test     # 아래 자동 항목 전부 (빌드 → 타입 검사 → 9개 스크립트)
```

실행 순서: 클라이언트 빌드 → 타입 검사 → 규칙 단위(머더미스터리) → 규칙 단위(카드 대전 엔진) → 서버 로직 → 재접속/관전/구역 → 서버 재시작 복원 → 2인 UI 플레이 → 3인 구역 격리 → 에디터.
브라우저 테스트는 Playwright(Chromium, 가짜 마이크 장치)로 **실제 창을 여러 개 띄워** 확인한다.

---

## 자동으로 확인되는 것

### 방 · 입장 — `smoke-server.mjs`
- [x] 방 생성 / 비밀번호 방은 비밀번호를 요구 / 틀린 비밀번호 거부
- [x] 여러 명 입장, 방 목록 반영
- [x] 앉지 않은 사람은 게임을 고를 수 없음
- [x] 전원 준비 전에는 자동 시작 카운트다운이 뜨지 않음 — `smoke-reconnect.mjs`
- [x] 엔딩 후 방이 대기 상태로 복귀

### 이동 · 월드 — `smoke-server.mjs`, `play-2p.mjs`
- [x] 순간이동 시도를 서버가 교정(`m:fix`)
- [x] 클릭 이동으로 테이블까지 걸어가 자동 착석

### 밀담 구역 격리 — `zones-3p.mjs` (3인 이상이어야 증명됨)
- [x] 구역 안 대화가 밖으로 새지 않음 (텍스트)
- [x] 구역 안 사람의 말풍선이 밖에 보이지 않음
- [x] 구역 밖에서도 **누가 그 안에 있는지는 보임**
- [x] 인원 제한 없이 세 번째 사람도 입장되고, 구역 밖에서 인원 수가 보임 — `smoke-reconnect.mjs`
- [x] 음성 연결이 같은 구역 사람끼리만 생성됨
- [x] 실제 오디오 바이트가 흐르는지 확인 (연결만 된 척하는 것 아님)
- [x] 구역을 나오면 연결 대상이 즉시 바뀜
- [x] 마이크 켬/끔 상태가 구역 밖 사람에게도 보임
- [x] 투표 단계에서 구역이 닫힘(시나리오 설정 시) — `unit-mm.test.ts`
- [x] 밀담이 닫힌 단계에도 구역에 들어갈 수 있고, "밀담이 되지 않습니다" 안내가 뜸 — `play-2p.mjs`

### 세션 · 재접속 · 영속 — `smoke-reconnect.mjs`, `smoke-restart.mjs`
- [x] 끊김 → 유예 시간 동안 "연결 끊김" 표시, 진행이 그 사람을 기다림
- [x] 유예 만료 → 부재 처리, 자동 확인으로 진행 계속
- [x] 같은 토큰으로 복귀 시 캐릭터·열람 상태·카드 잔여·위치·채팅 기록 복원
- [x] 게임 중인 방에 관전자로 입장 가능, 관전자는 비공개 정보를 받지 못함
- [x] **부재자의 자리는 비워 두지 않음** — 다른 사람이 못 들어오고, 라운지 목록도 같은 수를 보여주며, 복귀해도 정원을 넘지 않음
- [x] 새로고침 후 같은 캐릭터로 복귀 (브라우저 UI 기준) — `play-2p.mjs`
- [x] **서버 재시작 후에도** 방·게임·열람 상태 복원, 타이머 마감이 다운타임만큼 보정

### 정보 공개 · 누설 방지 — `unit-mm.test.ts`, `smoke-server.mjs`
- [x] 대상 지정 공개가 대상자에게만 도착
- [x] 대상이 아닌 캐릭터의 설정집은 **아무에게도** 전달되지 않음
- [x] 개인 단서는 열람 권한자가 대상에 없으면 전달되지 않음
- [x] 자기 설정집만 받음 / 관전자는 자료 없음
- [x] 개인 단서 격리(유언장)

### 테이블에 펼치기 — `smoke-server.mjs`, `play-2p.mjs`
- [x] 비공개 자료는 확인 없이 펼칠 수 없음 / 확인하면 공개됨
- [x] 같은 공간의 다른 참가자에게 같은 자료가 보임, 펼친 본인에게도 보임
- [x] 관전자에게는 보이지 않음
- [x] 펼친 사람이 밀담 구역에 들어가면 구역 밖에는 보이지 않고, 나오면 다시 보임
- [x] 펼친 사람만 걷을 수 있음 / 걷으면 모두의 화면에서 사라짐
- [x] 펼쳤다는 알림이 채팅에 남음
- [x] 보는 사람이 닫아도 "같이 보기 중" 띠가 남고, 눌러서 다시 열림 (브라우저)

### 봉투 — `play-2p.mjs`
- [x] 단계 전환으로 도착한 봉투가 저절로 펼쳐짐
- [x] 상시 우편함 패널이 화면에 없음
- [x] 복귀 후 이미 열어 본 봉투가 다시 봉인되지 않음

### 진행 · 타이머
- [x] 자료를 열기 전에는 "확인" 불가
- [x] 받은 자료가 없는 참가자는 바로 확인 가능
- [x] 연장 +2분, 10초 쿨타임 안에는 거부, 쿨타임 후 재연장 가능
- [x] 제한시간 종료 → 자동으로 다음 단계

### 투표 · 엔딩 — `unit-mm.test.ts`, `smoke-server.mjs`
- [x] 공개 투표는 투표자가 보이고, 익명 투표는 숨겨짐
- [x] 동률이면 진범이 포함돼도 실패 처리
- [x] 투표 불참 캐릭터는 투표 불가, 정원 계산에서도 제외
- [x] 부재자 확인 후 엔딩 도달, 결과 산출

### 카드 — `smoke-server.mjs`
- [x] 카드 사용 / 잔여 횟수 차감 / 다른 참가자에게 사용 이벤트 전달 / 채팅 알림
- [x] 남의 카드 사용 거부

### 촉감 UI (실제 드래그) — `play-2p.mjs`
- [x] 봉투 밀랍 봉인을 끌어 뜯기
- [x] 사건 파일(설정집) 열기 / 책장 넘기기
- [x] 단서 확대·돋보기
- [x] 카드를 끌어 사용
- [x] 투표 도장 드래그 + 길게 눌러 확정
- [x] **문서를 펼친 상태에서도 채팅 입력창이 실제로 눌림** (오버레이가 덮지 않음)

### 운영자 에디터 — `editor.mjs`
- [x] 틀린 비밀번호 거부 / 토큰 없이 API 접근 401
- [x] 실시간 검증 (인원 3명인데 캐릭터 2명 → 오류 감지)
- [x] 단서 이미지 업로드
- [x] 개인 단서의 실제 수신자 미리보기
- [x] 맵에서 드래그로 구역 생성 (인원 제한 입력란 없음)
- [x] 저장 즉시 게임 선반에 반영

### 카드 대전 — 전투 엔진 — `unit-cb.test.ts`

기준서 `claude/카드대전-기준서.md` 13번 목록. 1단계(엔진)에서 덮는 범위다.

- [x] 캐릭터 8명의 HP / Max EN, 기술 32장의 DM / EN / range 가 기준서 9번 표와 일치
- [x] **이름은 수치와 따로 검사한다** — 캐릭터 8명의 이름·이명·무기·소개, 기술 32장의 이름이 9번 표와 일치, 한 손패 14장 안에서 이름이 안 겹침
- [x] 손패 14장 (이동 6 + 방어·보조 4 + 기술 4), 범위 10종 전부 좌우 대칭
- [x] 캐릭터 선택: 제한 30초 / 확정 후 변경 불가 / 미선택 시 랜덤 배정 / **랜덤 배정은 자동 선택 카운트를 올리지 않음** / 둘 다 확정 시 동시 공개 / 동일 캐릭터 대전
- [x] 이동: 1칸·2칸, 경계에서 1칸만 가능·0칸 가능, 같은 칸 점유, 자리 바꿈, 이동 후 공격 명중
- [x] 공격 범위: 보드 밖 무시, 자기 피해 면제, 같은 칸의 상대 피해, 범위 밖 MISS
- [x] **MISS 여도 기력은 소모**
- [x] Guard −15 / Perfect Guard 무효, 같은 슬롯에만 적용되고 슬롯이 끝나면 해제
- [x] 공격 대 공격 동시 판정, 동시 사망 무승부, 사망 뒤 남은 슬롯 취소
- [x] Energy Up 상한, Heal 상한(+40), **턴 종료 기력 자동 회복 부재**
- [x] 제출 검증: 3장 / 중복 차단 / 손패 밖 카드 차단 / 순서별 기력 가상 실행 / 제출 잠금
- [x] 자동 선택이 유효한 3장을 만들고, 3연속이면 패배. **AI 는 예외**
- [x] **기력 0 카드만 내는 판이 20턴에 끝남**
- [x] **20턴 판정은 HP 비율 기준** — `150/190`(0.789)이 `160/205`(0.780)를 이긴다. 절대값이 아니다
- [x] 비율이 같으면 무승부 / 기권 시 상대 승리
- [x] 같은 seed 면 자동 선택이 재현됨, `BattleState` JSON 직렬화 → 복원 무손실

### 카드 대전 — 등록과 정보 차단 — `smoke-server.mjs`

2단계에서 덮는 범위. 소켓 클라이언트 3개(플레이어 2 + 앉지 않은 관전자 1)로 확인한다.

- [x] 모듈 목록에 카드 대전이 뜨고 **콘텐츠 2개**(`card-battle` 2인 고정 / `card-battle-solo` 1인)
- [x] 테이블에서 골라 준비하면 세션이 시작되고, 첫 단계가 캐릭터 선택
- [x] 내가 고른 캐릭터는 내 뷰에만 보이고, **상대와 관전자에게는 확정 전까지 `null`**
- [x] 둘 다 확정되면 동시 공개, 그 시점에 HP = Max HP (C1 하진 200 / C5 채령 190)
- [x] 제출 뒤 다시 낼 수 없음 / 관전자는 행동할 수 없음
- [x] 상대에게는 "선택 완료" 여부만 — **뷰 페이로드를 통째로 문자열 검사해 미공개 카드 id 가 없음을 확인**
- [x] 관전자 뷰 페이로드에도 어느 쪽 미공개 카드도 없음
- [x] 슬롯이 한 장씩 열리고, **아직 안 열린 슬롯의 카드는 관전자 뷰에도 없음**

### 카드 대전 — AI — `unit-cb.test.ts`

- [x] **AI 입력에 상대의 이번 턴 제출이 들어갈 자리 자체가 없음** (`ai/context.ts` 의 타입으로 강제)
- [x] Normal / Hard 둘 다 어떤 상태에서도 합법적인 3장을 낸다 (기력 0 포함)
- [x] Normal: 사거리 안이면 때리고, 멀면 다가간다
- [x] AI 대 AI 판이 끝까지 간다
- [x] **Hard 가 Normal 보다 많이 이긴다** — 이 항목이 `EN_VALUE` 조정의 안전망이다

### 카드 대전 — 화면 — `play-cb.mjs` (브라우저 2인)

- [x] 게임 선반에 카드 대전이 뜸 (서버 모듈 + `listContents()` 등록)
- [x] **카드 대전 화면이 올라옴** — 클라이언트 레지스트리 등록이 빠지면 여기서 잡힌다
- [x] ⓪ 8명 격자, 고른 캐릭터의 기술 4장, 상대 선택은 확정 전까지 `???`
- [x] **화면이 슬롯 표기가 아니라 이름으로 뜬다** — ⓪ 상세(하진)와 기술 목록(한 획/내려 긋기/열십자/엇결), ① 상단 바 양쪽(하진/채령), 보드의 말
- [x] ⓪ → ① → ② 화면 전환
- [x] ① 턴 수 `n / 20`, 손패 14장이 세 줄
- [x] **기력 바의 금색 빗금(예상)이 다음 턴의 실제 차감과 일치**
- [x] ② 전투 화면에서 **손패가 보이지 않음**, 턴 수는 같은 자리에 그대로
- [x] ② 아직 안 열린 슬롯은 뒷면으로 남음
- [x] 세 장을 다 열면 다음 턴으로

> 이 테스트가 실제로 잡은 것 둘: 채팅 패널이 카드 대전 화면 위를 덮어 클릭이 막히던 것(`.chat` 이 z-index 58), 말풍선이 `pointer-events` 를 먹어 그 아래 타일이 안 눌리던 것.

아직 덮지 않은 것:

- [ ] 서버 재시작 후 복구 — 다운타임이 제한 시간보다 짧으면 마감 밀림, 길면 재부여
- [ ] 끊김 중 타이머 진행 / `onParticipantAbsent` → 기권 + `endGame()`
- [ ] AI 연습 콘텐츠(`card-battle-solo`)의 브라우저 플레이와 난이도 전환

---

## 사람이 눈·귀로 확인해야 하는 것

자동화하기 어렵거나 자동화의 가치가 낮은 항목. **감각에 영향 주는 수정을 했을 때** 직접 실행해 확인한다.

- [ ] 효과음이 상황에 맞게 나는가 (종이·책장·카드·밀랍·도장·종·천둥·빗소리, 구역 안 먹먹한 필터)
- [ ] 아바타 커스터마이즈 결과가 월드에서 제대로 보이는가
- [ ] 조명·번개·비 연출이 과하거나 부족하지 않은가
- [ ] 단계 전환 연출의 호흡 (너무 빨라 놓치거나 너무 느려 답답하지 않은가)
- [ ] 엔딩 연출 순서 (표 집계 → 진범 공개 → 결과 → 진상·인물별 결말)
- [ ] 실제 마이크로 말했을 때 음성 품질·지연
- [ ] 모바일/좁은 화면 (아직 검증한 적 없음)
- [ ] 표현(이모티콘) 목록에서 연속으로 고를 때 손맛 — 눌린 칸이 튀어 오르는 정도가 적당한가
- [ ] 채팅 필터를 `대화`로 두고 한 판을 했을 때 놓치는 정보가 없는가
- [ ] 시나리오를 실제로 끝까지 플레이했을 때 **재미있는가** — 이게 최종 기준이다

---

## 테스트를 추가하는 위치

| 무엇을 검증하나 | 파일 |
| --- | --- |
| 머더미스터리 규칙 (브라우저 없이 빠르게) | `tests/unit-mm.test.ts` |
| 카드 대전 전투 판정 (서버·UI 없이 순수 함수) | `tests/unit-cb.test.ts` |
| 카드 대전 등록·뷰 차단 (소켓 프로토콜) | `tests/smoke-server.mjs` 끝의 「카드 대전」 블록 |
| 카드 대전 화면 (실제 UI 조작) | `tests/play-cb.mjs` |
| 서버 프로토콜 · 권한 · 전체 흐름 | `tests/smoke-server.mjs` |
| 끊김 · 복귀 · 관전 | `tests/smoke-reconnect.mjs` |
| 서버 재시작 복원 | `tests/smoke-restart.mjs` |
| 실제 UI 조작 · 촉감 | `tests/play-2p.mjs` |
| 구역 격리 (**반드시 3명 이상**) | `tests/zones-3p.mjs` |
| 운영자 에디터 | `tests/editor.mjs` |

> 구역 격리는 2명으로는 증명되지 않는다. "안에 있는 두 사람만 들린다"를 확인하려면 **밖에 있는 세 번째 사람**이 필요하다.
