# 이슈 · 요청 목록

사용자가 채팅으로 가볍게 말한 내용을 **증상 / 재현 / 기대 동작**으로 옮겨 적는 곳.
처리하면 완료 표시와 함께 **어떻게 고쳤는지**를 남긴다. 새 항목은 위쪽 `열림`에 추가한다.

상태: `열림` → `진행 중` → `완료` / `보류`

---

## 열림

### #6 TURN 서버 연동
- **유형** 기능 · **우선순위** 배포 단계에서
- **요청** 다른 망 사이에서도 음성이 붙게 한다
- **기대 동작**
  - 서버가 Metered REST를 호출해 자격증명을 받아 캐시하고, 기존 `/api/config`로 내려준다 — **API 키는 브라우저에 절대 노출하지 않는다**
  - 1순위 Metered(서울), 2순위 ExpressTURN(프랑스)을 한 목록으로 합쳐 전달
- **선행 조건** 없음 (키는 `.env`에 보관됨)

### #9 게임 상태를 외부 저장소로
- **유형** 구조 · **우선순위** 배포 단계에서 (필수)
- **문제** 지금은 메모리 + 3초마다 JSON 파일. 디스크가 휘발하는 무료 PaaS에서는 배포할 때마다 진행 중인 방이 사라진다
- **방향** 저장소를 인터페이스로 분리하고 파일 어댑터는 유지, 배포처가 정해지면 Redis/DB 어댑터를 끼운다. DB는 앱 서버와 같은 리전이어야 한다

### #10 장애 감지
- **유형** 운영 · **우선순위** 배포 단계에서
- **문제** 서버가 죽어도 사용자가 말하기 전까지 아무도 모른다
- **방향** 예약 작업으로 `/api/health`를 주기적으로 확인하고 이상하면 알림. 유지보수 스킬과 분리해 상시 동작

## 보류

### #7 발언 요청 표시
- **유형** 기능 요청
- **요청** 말하고 싶을 때 손을 드는 표시
- **보류 사유** 지금은 굳이 필요하지 않다는 판단 (2026-09-18). 나중에 다른 작업과 함께
- **하게 된다면** 2.6초짜리 일회성 이모티콘이 아니라 **켜고 끄는 상태 토글**. 참가자 목록에도 표시. ✋는 인사처럼 보이므로 전용 아이콘을 새로 그린다

### #8 재촉 이모티콘
- **유형** 기능 요청
- **보류 사유** 채팅으로 충분하다는 데 합의 (2026-09-18). 구현하지 않기로 결론

---

## 완료

### #12 부재자의 자리가 남에게 넘어가 정원을 넘김 — 완료 (2026-09-18)
- **증상** 관전 포함 정원 2명인 방. 게임 진행 중 1명이 나감 → 관전자 1명이 빈자리로 입장 → 나갔던 사람이 다시 들어와 3명이 됨
- **재현** 정원 N인 방에서 게임 시작 → 참가자 한 명이 "나가기" → 다른 사람이 입장 → 나갔던 참가자가 같은 브라우저로 재입장
- **원인** 정원 계산이 `연결됨 || !부재` 라서 부재 처리된 참가자는 자리를 차지하지 않는 것으로 셌다. 반면 복귀는 정원 검사를 건너뛴다(재접속 정책상 당연). 두 규칙이 어긋나 정원을 넘길 수 있었다
- **수정** 진행 중인 게임의 참가자는 부재 상태여도 자리를 유지하도록 했다. 부재자는 언제든 돌아와 이어서 플레이할 수 있으므로 그 자리는 비워 두지 않는 게 맞다. 게임이 끝나면 부재 참가자는 정리되어 자리가 풀린다(기존 동작)
- **함께 고친 것** 라운지 목록의 인원 수가 다른 규칙으로 세고 있어 `2/3`으로 보이는데 입장은 거부되는 어긋남이 있었다. 정원 계산을 `seatCount()` 한 곳으로 모아 목록과 입장 판정이 같은 값을 쓰게 했다
- **검사** `smoke-reconnect.mjs`: 부재자 자리를 다른 사람이 차지하지 못함 / 라운지 목록에도 가득 찬 것으로 보임 / 복귀 후에도 정원을 넘지 않음

### #11 밀담이 닫힌 단계에 구역에 들어가도 아무 안내가 없음 — 완료 (2026-09-18)
- **증상** 밀담 페이즈가 아닌데도 밀실에 입장이 돼서, 밀담이 되는 줄 알았다
- **확인 결과** 설계상 의도된 동작이었다. 구역 입장은 항상 가능하고 격리만 단계에 따라 켜진다. 다만 그 사실이 들어간 순간에 전달되지 않는 게 문제였다
- **수정** 격리가 꺼진 구역에 들어서면 `🔓 서재 — 지금은 밀담이 되지 않습니다. 여기서 한 말도 모두에게 들립니다` 안내를 띄운다. 구역 안에 서 있는 동안 단계가 바뀌어 격리가 꺼지는 경우도 같은 안내가 뜬다 (이전에는 "공용 공간으로 나왔습니다"라는 틀린 안내가 떴다)
- **검사** `play-2p.mjs`: 2단계에서 구역이 닫혀 있음 + 들어가지긴 하되 안내가 뜸

### #1 테이블에 펼치기 — 완료 (2026-09-18)
- **요청** 말로만 설명하는 것보다 PT 하듯 단서·문서를 모두 화면에 보여주고 싶다
- **구현** 단서 뷰어와 사건 파일에 「테이블에 펼치기」 버튼. 서버가 같은 대화 채널(공용 공간 또는 같은 밀담 구역)에 있는 참가자에게만 전달한다. 비공개 자료는 "정말 공개할까요? 되돌릴 수 없습니다" 확인을 거친다. 펼친 사람은 걷을 수 있고, 보는 사람은 각자 닫아도 화면 아래 "같이 보기 중" 띠가 남아 다시 열 수 있다. 관전자에게는 보이지 않는다
- **함께 고친 것** 구역을 드나들 때 게임 뷰를 다시 보내도록 수정 (이전에는 이동해도 이전 화면이 남아 있었다)
- **검사** `smoke-server.mjs` 8건(확인 없이 공개 거부, 같은 공간 전달, 관전자 차단, 구역 격리 양방향, 걷기 권한), `play-2p.mjs` 5건(상대 화면 표시, 닫아도 띠 유지, 다시 보기, 걷으면 사라짐)

### #2 봉투 UI 간소화 — 완료 (2026-09-18)
- **증상** 봉투 UI가 상시로 떠 있어 화면이 복잡해 보인다
- **구현** 상시 우편함 패널 제거. 단계 전환으로 자료가 오면 전환 밴드 뒤에 봉투가 자동으로 펼쳐지고, 그 외 타이밍이면 토스트만 뜬다. ESC·바깥 클릭으로 언제든 치울 수 있고 자료는 사건 파일·단서 보드에 남는다. 첫 봉투의 밀랍 뜯기 연출은 유지
- **검사** `play-2p.mjs`: 자동 개봉, 상시 우편함 없음

### #3 이모티콘 연속 클릭 — 완료 (2026-09-18)
- **구현** 골라도 목록이 닫히지 않는다. 고른 칸이 잠깐 튀어 올라 보냈다는 느낌을 준다. 닫기는 바깥 클릭·ESC·닫기 버튼·표현 버튼으로만

### #4 채팅 필터 — 완료 (2026-09-18)
- **구현** 채팅창 상단 `[전체] [대화] [시스템]` 칩. 고른 값은 로컬에 저장되어 새로고침해도 유지. 숨긴 개수를 오른쪽에 표시

### #5 밀담 구역 인원 제한 제거 — 완료 (2026-09-18)
- **구현** 서버 정원 검증, 에디터 입력란, 스키마 필드를 모두 제거. 구역 밖에서 보이는 `서재 · 2명` 표시는 유지
- **검사** `smoke-reconnect.mjs`: 세 번째 사람도 제한 없이 입장 / 구역 밖에서도 인원 수가 보임, `editor.mjs`: 입력란 없음


### #0 캐스팅 중 채팅·마이크가 보이지 않던 문제 — 완료 (2026-09-16)
- **증상** 배역을 고르는 화면에서 채팅창과 마이크 버튼이 보이지 않았다
- **재현** 방 입장 → 테이블 착석 → 게임 시작 → 캐스팅 화면
- **원인** 번들러가 `mm.css`를 별도 청크로 분리하면서 메인 CSS보다 먼저 로드되어, 같은 특이도의 `z-index` 규칙이 밀렸다. 문서·단서·투표 오버레이에서도 같은 증상이 조용히 생기고 있었다
- **수정** 셀렉터에 `.room-screen`을 붙여 특이도를 높이고, 캐스팅 화면에 좌우 여백을 줘서 채팅·컨트롤 바 자리를 비웠다
- **재발 방지** `tests/play-2p.mjs`에 "문서를 펼친 상태에서도 채팅 입력창이 눌린다" 검사를 추가 (`elementFromPoint`로 실제 클릭 대상 확인)
