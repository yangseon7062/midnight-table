# 이슈 · 요청 목록

사용자가 채팅으로 가볍게 말한 내용을 **증상 / 재현 / 기대 동작**으로 옮겨 적는 곳.
처리하면 완료 표시와 함께 **어떻게 고쳤는지**를 남긴다. 새 항목은 위쪽 `열림`에 추가한다.

상태: `열림` → `진행 중` → `완료` / `보류`

---

## 열림

### #1 테이블에 펼치기 (단서·문서 공유 발표)
- **유형** 기능 요청 · **우선순위** 높음
- **요청** 한 사람이 주도해서 말로만 설명하는 것보다, PT 하듯 단서·문서를 모두의 화면에 띄워 보여주고 싶다
- **기대 동작**
  - 참가자가 단서 또는 설정집 페이지를 "테이블에 펼치기" → 대상자 화면에 같은 자료가 동시에 뜬다
  - 공개 자료는 즉시 펼쳐진다
  - **비공개 자료는 되돌릴 수 없는 공개**이므로 확인 단계를 거친다 — "정말 공개할까요? 되돌릴 수 없습니다"
  - 관객 범위는 기존 밀담 구역 규칙을 그대로 따른다 (구역 안이면 그 구역 사람에게만)
  - 발표자도 닫을 수 있고 보는 사람도 각자 닫을 수 있다. 닫아도 "같이 보기 중" 배너는 남는다
- **설계 합의됨** (2026-09-18)

### #2 봉투 UI 간소화
- **유형** 개선 · **우선순위** 높음
- **증상** 봉투 UI가 상시로 떠 있어 화면이 복잡해 보인다
- **원인 분석** 미개봉 자료로 가는 입구가 셋인데 목적지는 하나다 — 우편함 패널 · 상단 📩 뱃지 · 사건 파일/단서 레일 뱃지
- **기대 동작**
  - 상시 우편함 패널(Mailbox) 제거
  - 봉투는 **도착하는 순간에만** 존재. 뜯으면 내용 → 닫으면 사건 파일 / 단서 보드로 들어간다
  - 놓친 자료는 레일 뱃지로 봉투 없이 바로 열린다
  - **정보 공개 단계 전환 시에는 자동 개봉** (단계 전환 밴드 → 봉투 → 자료로 한 흐름)
  - 그 외 타이밍에 자료가 생기면 자동 개봉 없이 `📩 봉투 도착` 토스트만
  - 자동으로 뜬 봉투도 ESC / 바깥 클릭으로 즉시 치울 수 있다
  - **첫 봉투(배역·설정집 수령)의 밀랍 뜯기 연출은 유지** — 손으로 만지는 유일한 도입부라 없애지 않는다
- **설계 합의됨** (2026-09-18)

### #3 이모티콘 연속 클릭
- **유형** 개선 · **우선순위** 중간
- **증상** 이모티콘을 하나 누르면 목록창이 닫혀서, 연속으로 쓰려면 매번 다시 열어야 한다
- **기대 동작** 누른 뒤에도 목록이 그대로 떠 있어 연속 클릭 가능. 닫기는 바깥 클릭 / 닫기 버튼 / ESC 로만

### #4 채팅 필터
- **유형** 기능 요청 · **우선순위** 중간
- **증상** 시스템 메시지가 대화와 섞여서 전부 보인다
- **기대 동작** 채팅창 상단에 필터 칩 `[전체] [대화] [시스템]`. 기본은 전체, 고른 값은 로컬에 기억
- **참고** 진행 기록(🕰) 패널이 이미 있어 시스템 메시지를 걸러도 정보 손실이 없다. 나중에 `[밀담]` 확장 여지

### #5 밀담 구역 인원 제한 제거
- **유형** 설계 변경 · **우선순위** 중간
- **결정** 구역 인원 제한을 두지 않는다 (2026-09-18)
- **기대 동작**
  - 정원 초과로 입장이 막히는 동작 제거 (서버 검증 · 에디터 "최대 인원" 입력란)
  - **구역 밖에서 인원 수가 보이는 표시는 유지** (예: `서재 · 2명`)
- **함께 고칠 것** `tests/smoke-reconnect.mjs`의 "2인실 정원 초과 차단" 검사를 "제한 없이 모두 입장된다"로 교체, `PROJECT.md` 설계 결정표 갱신

### #6 TURN 서버 연동
- **유형** 기능 · **우선순위** 배포 단계에서
- **요청** 다른 망 사이에서도 음성이 붙게 한다
- **기대 동작**
  - 서버가 Metered REST를 호출해 자격증명을 받아 캐시하고, 기존 `/api/config`로 내려준다 — **API 키는 브라우저에 절대 노출하지 않는다**
  - 1순위 Metered(서울), 2순위 ExpressTURN(프랑스)을 한 목록으로 합쳐 전달
- **선행 조건** 없음 (키는 `.env`에 보관됨)

---

## 보류

### #7 발언 요청 표시
- **유형** 기능 요청
- **요청** 말하고 싶을 때 손을 드는 표시
- **보류 사유** 지금은 굳이 필요하지 않다는 판단 (2026-09-18). 나중에 다른 작업과 함께
- **하게 된다면** 2.6초짜리 일회성 이모티콘이 아니라 **켜고 끄는 상태 토글**. 참가자 목록에도 표시. ✋는 인사처럼 보이므로 전용 아이콘을 새로 그린다

### #8 재촉 이모티콘
- **유형** 기능 요청
- **보류 사유** 채팅으로 충분하다는 데 합의 (2026-09-18). 구현하지 않기로 결론

---

## 완료

### #0 캐스팅 중 채팅·마이크가 보이지 않던 문제 — 완료 (2026-09-16)
- **증상** 배역을 고르는 화면에서 채팅창과 마이크 버튼이 보이지 않았다
- **재현** 방 입장 → 테이블 착석 → 게임 시작 → 캐스팅 화면
- **원인** 번들러가 `mm.css`를 별도 청크로 분리하면서 메인 CSS보다 먼저 로드되어, 같은 특이도의 `z-index` 규칙이 밀렸다. 문서·단서·투표 오버레이에서도 같은 증상이 조용히 생기고 있었다
- **수정** 셀렉터에 `.room-screen`을 붙여 특이도를 높이고, 캐스팅 화면에 좌우 여백을 줘서 채팅·컨트롤 바 자리를 비웠다
- **재발 방지** `tests/play-2p.mjs`에 "문서를 펼친 상태에서도 채팅 입력창이 눌린다" 검사를 추가 (`elementFromPoint`로 실제 클릭 대상 확인)
